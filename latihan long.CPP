#include <iostream.h>
#include <conio.h>
#include <stdio.h>
#include <iomanip.h>

Unsigned log factorial (unsigned log);
int i, number;

main()
{
{
for(int i=0;i<=10;i++) ;
cout<<setw(2)<<i<<"! Faktorial(i)"<<endl;
return 0;
}
{
if(number<=1)
return 1;
else
return number * factorial(number -1);
}

getch();
}
